import 'package:flutter/material.dart';

class ProductListPage extends StatelessWidget {
  // final Function removeProduct;
  // ProductListPage(this.removeProduct);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: RaisedButton(
        child: Text("Delete"),
        onPressed:(){},
      ),
    );
  }
}
